apiVersion: apps/v1
kind: Deployment
metadata:
  name: "hello-deployment"
  namespace: "hello-app"
spec:
  selector:
    matchLabels:
      app: "hello"
  replicas: 4
  template:
    metadata:
      labels:
        app: "hello"
    spec:
      containers:
      - image: 324382802360.dkr.ecr.eu-west-1.amazonaws.com/ecr-gui:2.1
        imagePullPolicy: Always
        name: "hello"
        ports:
        - containerPort: 8000
